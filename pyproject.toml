[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "scikit-build>=0.15.0",
    "cmake>=3.22",
    "pybind11~=2.9.2",
    "ninja; platform_system!='Windows'",
]

build-backend = "setuptools.build_meta"