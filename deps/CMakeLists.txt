INCLUDE (CPM.cmake)

# ADD_SUBDIRECTORY (boost)
ADD_SUBDIRECTORY (scrtdd)

SET_PROPERTY (TARGET boost_filesystem PROPERTY POSITION_INDEPENDENT_CODE ON)
TARGET_LINK_LIBRARIES (hdd PRIVATE boost_filesystem)
